##### 1. 다음 ( 괄호 ) 안에 들어갈 명령으로 알맞은 것은?

```
# ls -l lin.txt
-rwxrwxrwx. joon ihd 513 DEc 22 21:05 lin.txt
# ( 괄호 )
#ls -l lin.txt
-rwcrwcrwx. joon kait 513 Dec 22 21:05 lin.txt
```

- 파일의 소유자가 ihd에서 kait로 바뀐 상황이다.

- `ls -l [파일명]`: 소유권 정보를 볼 수 있다. 첫 부분은 허가권, 다음은 소유권을 가진 사용자, 그 다음은 그룹 소유권이다. 첫번 째 영역은 파일 타입, 그 다음은 3자리씩 끊어서 소유자에게 적용되는 권한, 두 번째는 해당 그룹에 속한 사용자들에게 적용되는 권한, 나머지는 다른 사람들(other)에 적용되는 권한이다. r은 읽기 w는 쓰기 x는 실행(디렉토리의 경우 디렉토리에 접근할 수 있는 권한)을 의미한다.

- [ ] `chown :ihd lin.txt`

- [x] `chown :kait lin.txt`

- [ ] `chgrp ihd lin.txt`
  
- [ ] `chgrp :kait lin.txt`

##### 2. 다음 중 저널링 파일 시스템을 생성하는 명령으로 틀린 것은?

- 저널링 파일 시스템: 주 파일 시스템에 변경사항을 반영하기 전에 변경사항을 추적하여 저널에 저장하는 시스템. ext3와 ext4가 가장 자주 쓰인다.

- [x] `mke2fs /dev/sdb1`
- [ ] `mkfs -t ext3 /dev/sdb1`
- [ ] `mke2fs -j /dev/sdb1`
- [ ] `mke2fs -t ext4 /dev/sdb1`

##### 3. 다음 ( 괄호 ) 안에 들어갈 허가권 값으로 알맞은 것은?

```
# ls -l lin.txt
--w-------. joon ihd 513 Dec 22 21:05 lin.txt
# chmod 577 lin.txt
# ls -l lin.txt
( 괄호 )joon ihd 513 22 21:05 lin.txt 
```

- `chmod`: 파일이나 디렉토리에 접근할 수 있는 허가권을 설정한다. 문자모드와 숫자 모드가 있다.
  
- 문자모드 : rwx로 표기하며, 사용자 지정은 u,g,o,a로 표기한다.(a는 all) = 기호는 기존 권한에 덮어 쓰기, -는 권한 해제, +는 기존 권한에 더하기 이다.
  
    ```bash
    chmod ugo+rwx aa.txt
    chmod a+rwx aa.txt
    // 모든 사용자에게 모든 권한을 주는 같은 명령어
    ```
```
    
- 숫자 모드: 일의 자리는 other, 그룹은 십의자리, 백의 자리는 소유자로 지정하며, 읽기권한은 4, 쓰기 권한은 2, 실행권한은 1, 권환 없음은 0으로 이를 엮여서 표현한다.
    
    ```bash
    chmod 777 aa.txt
    // 위와 같다
```

- 특수 권한: `Set-UID`, `Set-GID`의 경우, 문자모드는 s를 사용하고, `Sticky-bit`는 t를 사용한다. 숫자 모드의 경우 천의 자리에 각각 4,2,1,을 사용한다.
  
- [x] -r-xrwxrwx

##### 4. 다음 중 스티키 비트(Sticky-bit)을 설정하는 방법으로 알맞은 것은?

- `Sticky-bit`는 other을 위한 특수 권한이다
- [x] `chmod o+t data/`

##### 5. 다음 중 파일에 부여되는 허가권인 w에 대한 설명으로 틀린 것은?

- [x] 파일을 삭제할 수 있다.
- [ ] 파일의 내용을 수정할 수 있다.
- [ ] 파일의 내용을 전부 비워서 빈 파일을 만들 수 있다.
- [ ] `cat`명령과  >>를 이용해서 파일의 내용을 추가할 수 있다.

##### 7.다음 중 `/etc/fstab`의 첫 번째 필드 형식으로 틀린 것은?

- `/etc/fstab`: 부팅 시에 마운트할 파티션 정보가 기록되어 있는 파일, 6개의 필드로 구성되어 있다.
    - 장치명 기록 : 볼륨 라벨(`LABEL=/`)이나 UUID도 사용가능
    - 마운트 될 디렉토리
    - 파일 시스템 유형
    - 마운트 옵션
    - `dump`를 통한 백업 주기 설정, 0이명 사용 안함, 1이면 하루 단위, 2면 이틀 단위로 수행한다.
    - 부팅시 `fsck`로 점검 순서, 0 이면 점검하지 않는다.
- [x] / : 마운트 될 디렉토리에 해당하는 두 번째 필드 형식이다.

##### 8. 다음에 대한 설명으로 틀린 것은?

```
# mke2fs -j -b 4096 -R stride=32 /dev/md0
```

- [ ] `/dev/md0`는 Raid 장치이다.
- [ ] 블록 사이즈는 4096바이트로 설정한다.
- [ ] stripe 당 블록 사이즈는 32바이트로 설정한다.
- [ ] `/dev/md0`를 ext2 파일 시스템으로 생성한다 : `-j`가 붙어 있으므로 ext3 저널링 시스템으로 만든다.

##### 9. 다음 중 `/home` 영역에 설정된 사용자 쿼터 정보를 출력하는 명령으로 알맞은 것은?

- [ ] `edquota` :사용자나 그룹에 쿼터를 설정하는 명령어, vi로 설정
- [x] `quotaon/quotaoff` : 쿼터 설정을 활성화/비활성화 하는 명령, 디렉토리로 지정
- [ ] `repquota`: 파일 시스템 단위로 쿼터 설정 정보를 출력해주는 명령이다.
- [ ] `quota` : 사용자 단위로 쿼터 설정 정보를 출력해주는 명령어
- [ ] `setquota` : 쿼터를 설장하는 명령어, 직접 명령행에서 입력가능

##### 10. 다음 중 DVD 등 이동식 보조기억장치의 미디어를 꺼낼 때 사용하는 명령으로 알맞은 것은?

- [ ] `fdisk` : 디스크 파티션 조작/확인
- [ ] `mkfs` : 새로운 파일 시스템 할당
- [x] `eject` : 이 명령어를 실행하면 자동으로 마운트도 해제한다.
- [ ] `unmount` : 마운트 도니 장치 해제

##### 11. ihd 사용자가 셸 프롬프트를 다음과 같이 변경하려 할 때 알맞은 것은?

```
[ihd@09:30:21 ~]$
```

- `PS1`: 터미널이 기본 시행되었을 때 관련있는 환경 변수
- `PS2`: PS1 터미널에서 추가로 실행되는(명령어를 추가로 입력 받아야 한다던지) 터미널의 환경변수
- 위의 `PS1`은 사용자 이름, 시간, 작업중인 디렉토리의 마지막 부분을 보여준다.

- [x] `PS1=["\u@\t \W"]`

##### 14. 다음 중 조건에 맞는 명령어 형식으로 알맞은 것은?

> - 메일 전송 시 mail 명령어 사용
> - webmaster@exmaple.com 계정에게 메일 전송
> - 메일 제목은 '[OK] server check'
> - 메일 내용은 report.txt 파일의 내용
> - 단, 하나의 라인으로 작업 완료

- `mail`: 쉘 상에서 메일을 보내는 명령어이다.
- [ ] `mail -s "[OK] server check" webmaster@example.com < report.txt`

##### 16. 다음 중 본 셸(sh)에 대한 설명으로 틀린 것은?

- [ ] AT&T의 벨 연구소의 스티븐 본이 개발하였다.
- [ ] 1977년에 처음으로 유닉스 버전 7에 포함되었다.
- [x] 조건구문(if), 반복구문(while)이 존재하지 않는다.
- [ ] 별칭이 존재하지 않는다.

##### 17. 다음 중 ( 괄호 ) 안에 들어갈 명령어로 알맞은 것은?

> 변수를 선언할 때는 '변수명=변수값' 형식을 사용하고, 선언된 변수를 해제할 때는, ( 괄호 ) 명령어를 사용한다.

- [x] unset : 다른 세가지는 존재하지 않는 커맨드이다.
- [ ] remove
- [ ] erase
- [ ] delete

##### 18. 다음 중 배시셸의 특성으로 틀린 것은?

- [ ] 브라이언 폭스가 GNU 프로젝트를 위해 개발
- [x] 리눅스 운영체제에서만 사용가능
- [ ] 명령어 완성 기능, ㅁ여령어 치환 기능 지원
- [ ] POSIX와도 호환

##### 23. 다음 중 시그널과 관련된 키보드입력에 대한 종류가 틀린 것은?

- [x] `SIGKILL : <CTRL + U>` : 기본 키 맵핑 없음
- [ ] `SIGINT : <CTRL + C>` : 프로세스에 인터럽트(차단)
- [ ] `SIGQUIT : <CTRL + \>`
- [ ] `SIGSTP : <CTRL + Z>`

##### 24. 다음 그림에서 PID가 9473인 프로세스의 NI값을 -10으로 변경하기 위한 명령으로 알맞은 것은?

- NI : 프로세스 실행 순위
- `nice` : 프로세스 명으로 NI 변경함
- `renice` : PID로 NI 변경함
- [x] `renice -10 9473`

##### 25. 다음 설명에 가장 알맞은 것은?

> 프로세스가 메모리에 항상 상주하지 않고 서비스 요청이 들어왔을 때 관련 프로세스를 실행시키고 완료 후 종료시키는 방식으로 효율적인 메모리 관리에 유용하다.

- [ ] fork : 새로운 프로세스를 위해 메모리를 할당받아 복사본 형태로 실행한다.
- [ ] exec : 원래의 프로세스를 새 프로세스로 덮어쓴다.
- [x] inetd : 데몬 프로세스를 서비스 요청이 들어올 때만 실행
- [ ] standalone : 데몬 프로세스를 상주 시킨다.

##### 26. 프로세스 상태를 출력해주는 명령어가 아닌 것은?

- [ ] `ps`
- [ ] `pstree`
- [ ] `kill` : 프로세스를 종료하는 명령어이다.
- [ ] `top`

##### 28. 다음 중 프로세스 유틸리티 사용법과 설명으로 틀린 것은?

- [ ] `kill -l` : 시그널의 종류를 출력
- [x] `killall - 9 1234` : PID가 1234인 프로세스에게 9번 시그널을 보냄

- `killall`은 프로세스 명을 입력해야 한다.
- [ ] `kill -HUP 1234` : PID ??????
- [ ] `kill 1234`

##### 29. 다음 중 vi 편집기를 이용하여 현재 커서의 취이부터 문서 끝까지 ihd라는 문자열은 kait로 치환하는 방법으로 알맞은 것은?

- [ ] `:% s/ihd/kait/g`

- %는 파일 전체를 의미한다. s는 찾기, g는 전역, ihd/kait는 찾을 문자열 바꿀 문자열을 의미한다.
- [ ] `:0,$ s/ihd/kait/g` 

- vi는 1부터 라인넘버를 세지만 어쨌든 파일 전체 교체
- [x] `:.,$ s/ihd/kait/g` 

- `.` 현재 라인

- [ ] `1,$ s/ihd/kait/g`

##### 30. vi 에디터를 이용해 readme.txt 파일을 열면서  커서를 마지막 줄에 둘 때 사용하는 명령으로 알맞은 것은?

- [ ] `vi -R readme.txt` : 읽기 전용을 열기
- [x] `vi + readme.txt`

- [ ] `vi -r readme.txt` : 복구 옵션
- [ ] `vi -c readme.txt` : `-C`는 compatible 열기, 소문자는 옵션 없음

##### 32. 다음 중 vi 편집기에서 입력모드 전환과 관련된 명령으로 가장 거리가 먼 것은?

- [ ] `i` : 현재 커서 위치에서 입력모드 전환
- [ ] `o` : 현재 커서 위치에서 빈 줄을 추가하고 전환
- [ ] `p`: paste
- [ ] `s`: 현재 커서 위치의 글자를 지우고 전환

##### 35. 다음 중 수세 리눅스에서 사용하는 저장소 기반의 패키지 관리 프로그램으로 알맞은 것은?

- [ ] yum
- [ ] apt-get
- [ ] YaST

- [x] Zypper

##### 36. 다음 ( 괄호 ) 안에 ㄷ들어갈 내용으로 알맞은 것은?

```
# rpm ( 괄호 ) vftpd
vsftpd-2.2.2-24.el6.i686
```

- [ ] `-v` : 명령어 실행 중 과정을 디테일 하게 보여준다.
- [ ] `-V` : 패키지 검증
- [x] `-q` : 쿼리모드
- [ ] `--version` : rpm의 버전 출력

##### 38. 다음 ( 괄호 ) 안에 들어갈 내용으로 알맞은 것은?

> 데비안 리눅스에서 패키지 설치 시에 발생할 수  있는 의존성을 해결하기 위해서는 ( ㉠ ) 을 이용하고, 레드햇 리눅스에서는 ( ㉡ ) 을 사용한다.

- [x] ㉠ `apt-get`  ㉡ `yum`

##### 39. 특정 패키지가 설치한 파일 목록을 확인 하려고 한다. ( 괄호 ) 안에 들어갈 내용으로 알맞은 것은?

```
# dpkg ( 괄호 ) vim
```

- [ ] `-ql`
- [ ] `-l` : 설치하는 패키지 목록
- [x] `-L`
- [ ] `-R`

##### 41. 다음 조건과 같은 때 ( 괄호 ) 안에 들어갈 내용으로 알맞은 것은?

> [조건]
>
> 현재 시스템에 설치된 패키지만 찾아서 업그레이드 설치하고, 메시지와 진행 상황을 '#' 기호로 출력한다.

```
# rpm ( 괄호 ) /usr/local/src/*.rpm
```

- `-U` 는 기존에 없을 경우 설치까지 하지만, `-F` 는 설치된 패키지만 업데이트 한다. `-v` 는 자세한 정보를 출력하고, `-h`는 진행상황을 '#'으로 표현한다.

- [x] `-Fvh`

##### 42. rpm 명령의 결과가 다음 그림과 같을 때 ( 괄호 ) 안에 들어갈 내용으로 알맞은 것은?

``` 
# rpm ( 괄호 ) passwd
```

- [x] `-qi`

##### 44. 다음 중 ALSA 사운드카드 장치를 초기화 하는 명령으로 알맞은 것은?

- ALSA : 사운드 카드 관련 요소를 제공하는 리눅스 커널의 요소
- `alsactl` : 사운드 카드 제어 명령어
- `alsammixer` : ALSA 시스템을 사용하는 오디오 카드의 믹서

- [ ] `alsactl reload`
- [x] `alsactl init`
- [ ] `alsamixert reload`
- [ ] `alsamixer init`

##### 47. 다음 중 사운드 카드와 관련이 없는 것은?

- [ ] ALSA
- [ ] OSS
- [x] SANE : 스캐너 처리를 위한 API
- [ ] OSS/free

##### 49. 다음 설명으로 알맞은 것은?

> 기존의 X를 대체할 목적으로 등장한 프로젝트로 개발 및 유지보수를 쉽게 하려는 것이 주목적이다. 클라이언트가 해당 프로토콜의  C 라이브러리 구현뿐만 아니라 클라이언트와 대화할 수 있는 compositor를 위한 프로토콜이다.

- [ ] X.org
- [ ] XFree86
- [x] Wayland
- [ ] Athena

##### 50. 다음 중 GUI 환경을 이용하기위해 사용자에게 제공되는 인터페이스 스타일에 해당하는 명칭으로 알맞은 것은?

- [x] 데스크톱 환경
- [ ] X 프로토콜
- [ ] 디스플레이 매니저
- [ ] 디스플레이 서버

##### 51. 콘솔 모드에서 두 번째 X 윈도를 실행시키려고 한다. 다음 ( 괄호 ) 안에 들어갈 내용으로 알맞은 것은?

```
# ( 괄호 ) -- :1
```

- [ ] `startx`
- [ ] `XClinets`
- [ ] `Xresources`
- [ ] `Xmodmap`

##### 52. 다음 그림에 해당하는 프로그램으로 알맞은 것은?

- [ ] Totem

##### 55. 다음 중 이미지 뷰어 프로그램으로 알맞은 것은?

- [x] eog
- [ ] Totem
- [ ] KMid
- [ ] Rhythmbox

##### 57. 다음 중 CentOS 6 버전에서 X 윈도 기반으로 네트워크 주소를 설정할 때 사용하는 명령으로 알맞은 것은?

- [ ] `netonf`
- [ ] netconfig
- [ ] system-config-network
- [ ] nm-connection-editor

##### 59. 다음 설명에 해당하는 파일로 알맞은 것은?

> 외부의 DNS 서버를 이용했으나, 회사 내부에 자체적으로 DNS 서버를 구축해서 해당 주소로 변경하려고 한다.

- [ ] /etc/sysconfig/network : 레드햇 계열에서 호스트명, 기본 게이트 웨이, IPv4, v6 사용 여부 기록
- [ ] /etc/hosts : DNS 서버가 보급되기 전에는 여기다 입력해서 사용함
- [ ] /etc/host.conf : 해당하는 IP주소를 어디서 찾을 지 기록
- [x] /etc/resolv.conf

61. ##### 다음 중 로컬 시스템에 사용 중인 네트워크 카드의 맥 주소를 확인할 때 사용하는 명령으로 알맞은 것은?

- [x] `ip`
- [ ] `ss` : 소켓 정보 보기
- [ ] `route` : 연결된 라우터, 게이트웨이 정보
- [ ] `arp` :

##### 63. 다음 중 원격지에 있는 시스템과의 프린터 공유를 위해 필요한 서비스로 알맞은 것은?

- [ ] SSH
- [ ] NFS
- [ ] 텔넷
- [x] SAMBA

##### 64. 다음 중 텔넷 명령을 이용해서 로컬 시스템의 웹 서버 동작을 확인 할 때의 명령으로 알맞은 것은?

- [x] `telnet localhost:80`

##### 65. 다음 ( 괄호 ) 안에 들어갈 내용으로 알맞은 것은?

> IPv4 ( ㉠ ) 비트 체제여서 현재 모든 IP 주소가 소진된 상태이다. 이러한 문제를 해소하기 위해 IPv6는 ( ㉡ ) 비트로 주소 체제를 확장하였다.

- [x] ㉠ 32 ㉡ 128

##### 66. 다음 중 루프백 네트워크가 속해 있는 IPv4의 클래스로 알맞은 것은?

- 루프백 아이피 : 자기 자신을 가리키는 IP, IPv4에서는 127.0.01이다.
- [x] A 클래스

##### 67. 다음 설명에 해당하는 LAN 구성 방식으로 알맞은 것은?

> 네트워크 전송상의 충돌이 없고, 노드의 숫자가 늘더라도 전체적인 성능 저하가 적다. 단점으로는 노드의 추가가 비교적 어렵고, 노드에 문제가 발생했을 경우에 전체 네트워크가 중단될 수 있다.

- [x] 링형

##### 68. 다음 중 'Sliding Window'와 관련 있는 프로토콜의 기능으로 알맞은 것은?

- [x] 흐름제어

##### 70. 다음 중 시스템에 설정된 게이트웨이 주소를 확인할 때 사용하는 명령으로 알맞은 것은?

- [ ] `ifconfig`
- [ ] `ss`
- [ ] `route`
- [ ] `arp`

##### 72. 다음 중 리눅스와 리눅스 시스템간의 디렉터리를 공유할 때 가장 효율적인 서비스로 알맞은 것은?

- [ ] SSH
- [x] NFS
- [ ] FTP
- [ ] SAMBA

##### 73. 다음 설명에 해당하는 파일로 알맞은 것은?

> 1987년에 DNS가 소개되기 전까지 유닉스 계열 시스템에서는 친숙한 컴퓨터 이름을 IP 주소에 매핑하는 형태로 이 파일에 등록해서 사용하였다.

- [x] /etc/hosts

##### 74. 다음 중 IPv6에 대한 설명으로 틀린 것은?

- [ ] 패킷 크기의 확장
- [x] IP 주소 대역 구분인 클래스의 확장
- [ ] 헤더 구조의 단순화
- [ ] 흐름 제어 기능 지원

##### 78. 다음 설명에 해당하는 시스템으로 알맞은 것은?

> 1990년대 초 CESDIS에서 Cray 슈퍼 컴퓨터의 임대 기간 종료에 대비하기 위한 프로젝트에서 탄생되었다. 고성능의 계산 능력을 제공하기 위한 목적으로 제작되었으며 주로 과학 계산요으로 사용된다.

- [x] 베어울프 클러스터

##### 79. 다음 설명과 관련 있는 기술로 알맞은 것은?

> 기존의 DB 관리도구로 데이터를 수집, 저장, 관리, 분석할 수 있는 역량을 넘어서 대량의 정형 또는 비정형 데이터 집합 및 이러한 데이터로부터 가치를 추출하고 결과를 분석하는 기술을 말한다.

- [x] 빅데이터